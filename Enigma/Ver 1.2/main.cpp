#include <iostream>
#include "EnigmaClass.h"

using namespace std;

int main()
{
    rotor R1;

    alpha test;
    alpha msg = A;
    test = R1.InOut(msg);
    int testi = (int) test;

    cout << "The converted msg is" << testi << endl;
    
}